pipeline {
  stages {
    stage('Dep install') {
      steps {
        sh(script: 'npm install')        
      }
    }
    stage('Test run') {
      steps {
        sh(script: 'node_modules/.bin/cypress run')          
      }
    }
  }
}